# 이주영

**Frontend Engineer | React, Next.js, React Native**

juyung0903@naver.com | https://github.com/phnml1 | https://phnml1.github.io/

## Summary

React, Next.js, React Native 기반 프로젝트에서 실시간 동기화, 캐시 갱신, 렌더링 성능 개선을 통해 사용자 경험을 개선해온 프론트엔드 엔지니어입니다.

댓글 동기화 구조 개선으로 API 요청 10회 → 0회, 처리 시간 21초 → 1.2초를 기록했고, Drag & Drop 화면에서는 React Profiler 기준 total commit duration 211.5ms → 72.9ms로 개선했습니다. 정량적 측정으로 병목을 확인하고, 상태 구조와 데이터 흐름을 분리해 응답 지연과 화면 불일치를 줄이는 작업에 강점이 있습니다.

## Skills

- Frontend: TypeScript, React, Next.js, React Native
- State & Data: TanStack Query, Zustand
- Realtime: WebSocket(STOMP), Socket.io, FCM
- Test: Jest

## Projects

### CodeMate

_AI 분석 요청과 실시간 협업 기능을 포함한 코드 리뷰 플랫폼_

**개인 프로젝트 | 2026.02 - 2026.04**  
**GitHub:** https://github.com/phnml1/CodeMate

GitHub PR 리뷰, AI 분석 요청, 댓글, 알림 흐름을 연결한 코드 리뷰 협업 플랫폼

#### 실시간 댓글 동기화 구조 개선

- 댓글 이벤트마다 전체 refetch가 발생하던 구조를 Socket 기반 부분 업데이트 + TanStack Query `setQueryData` + polling fallback 구조로 개선
- **API 요청 10회 → 0회**, **처리 시간 21초 → 1.2초**, 평균 latency 3.57ms / p95 7.2ms 측정
- 댓글 반영 대기 시간을 줄여 협업 흐름이 끊기지 않도록 개선

#### AI 리뷰 비동기 상태 흐름 및 실시간 구조 정리

- 상태 머신(PENDING → IN_PROGRESS → COMPLETED / FAILED) 설계로 요청 상태를 단계별로 노출하고 중복 요청 방지
- socket store와 hook으로 연결/구독 책임을 분리하고, **Jest 19개 suite / 115개 테스트**로 재연결·구독 복구 흐름 검증

---

### Career Hub

_취업 지원 현황 관리 플랫폼_

**팀 프로젝트 (FE 2 / BE 2) | 2025.11 - 2026.02**  
**GitHub:** https://github.com/phnml1/Career_Hub_Frontend

역할: 지원 상세 캐시 전략 및 전형 상태 동기화 구조 설계

지원 현황, 전형 일정, 웹 푸시 알림을 한곳에서 관리하는 취업 준비용 PWA

#### 전형 결과와 현재 단계 동기화 구조 설계

- 서류·기타·면접 결과 변경 시 현재 단계와 최종 결과가 일관되게 갱신되도록 규칙을 공통 로직으로 분리
- FE와 BE의 상태 enum, 예외 해석 기준을 다시 정리해 동일 지원 건이 화면마다 다르게 보이던 문제를 개선
- 전형 수정 훅 전반에서 동일 기준을 재사용해 화면 간 상태 정합성 확보

#### 수정 유형별 React Query 캐시 갱신 전략 분리

- 상세 수정 후 전체 재조회 대신, 응답만으로 안전하게 갱신 가능한 항목은 `setQueryData`, 파일 업로드처럼 서버 재생성이 필요한 경우만 `invalidateQueries` 적용
- **상세 refetch 1회 → 0회**
- 수정 직후 변경값을 즉시 반영하면서 캐시 정확성 유지

---

### Fly:On

_체험 비행 일정 관리 앱_

**팀 프로젝트 (FE 2 / BE 2 / Design 1) | 2025.06 - 2025.11**  
**GitHub:** https://github.com/SQUAD-FLY-ON/FLY-ON

역할: 일정 편집 성능 최적화 및 상태 구조 설계

#### Drag & Drop 렌더링 병목 개선

- 드래그 중 상태 변경이 넓은 렌더링 범위로 전파되며 commit duration 증가와 프레임 저하가 발생
- React Profiler 기반 병목 분석 후 렌더링 범위를 제한하는 구조 적용
- **total commit duration 211.5ms → 72.9ms**, **평균 commit duration 17.6ms → 6.6ms**
- 일정 편집 조작의 안정성 개선

#### 편집 상태와 서버 캐시 분리

- UI 상태(Zustand)와 서버 캐시(TanStack Query)의 역할을 분리해 편집 중 입력값이 서버 응답에 의해 덮어써지지 않도록 개선
- 조회 조건 제한으로 불필요한 API 호출을 줄이고, 공통 에러 처리 구조로 화면별 예외 분기를 축소

---

### 러너스하이

_위치 기반 실시간 러닝 앱_

**졸업작품 (FE 1 / BE 2) | 2025.02 - 2025.07**  
**GitHub:** https://github.com/SQUAD-RUNNERS-HIGH/frontend

역할: 프론트엔드 개발 전담

#### 실시간 위치 동기화 복구 구조 설계

- 연결 끊김 시 위치 동기화가 중단되던 문제를 heartbeat, reconnectDelay 기반 자동 복구 구조로 개선
- 연결 재수립 이후 위치 동기화 흐름이 재개되도록 구현

#### 페이스 계산 정확도 개선

- GPS raw speed 기반 계산 시 값 변동이 커 저속 구간에서 pace가 불안정하게 표시되던 문제를 개선
- 최근 10초 이동 거리 기반 평균 pace 계산 적용으로 급격한 값 변화 감소

## Education

한국공학대학교 공학사 | 2019.03 - 2026.02

복수전공: 소프트웨어학과 | 주전공: IT경영
